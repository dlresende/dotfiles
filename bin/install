#!/usr/bin/env bash

[ -z "$DEBUG" ] || set -x

set -e # bail out early if any command fails
set -u # fail if we hit unset variables
set -o pipefail # fail if any component of any pipe fails

( cd "$HOME" || exit 1
  git clone https://github.com/dlresende/dotfiles.git .
  ./bin/pimp-my-ride
)
